<form method="post" action="{{ path('send_custom_email') }}">
    <label for="email">Email du destinataire :</label>
    <input type="email" id="email" name="email" required>
    <button type="submit">Envoyer l'email</button>
</form>